# -*- coding: utf-8 -*-
"""
app/config.py
---------------
รวมค่าคงที่ (Configuration) ของระบบไว้ที่เดียว
เพื่อให้ปรับแก้ค่าต่าง ๆ ได้จากจุดเดียว ไม่ต้องไล่หาเลข hardcode กระจายในหลายไฟล์
"""

# ---------------------------------------------------------------------------
# ระยะเวลาสูงสุดที่อนุญาตให้ "ไปห้องน้ำ" ได้ ก่อนถูกตีสถานะเป็น Missing
# ของจริงตาม requirement คือ 15 นาที (900 วินาที)
# ตอนเทส/พรีเซนต์ ให้ลดเป็นค่าน้อย ๆ เพื่อให้เห็นผลไว ไม่ต้องรอนาน
# ---------------------------------------------------------------------------
AWAY_TIMEOUT_SECONDS = 15            # <-- ใช้ค่านี้ตอนเทส/พรีเซนต์ (15 วินาที)
# AWAY_TIMEOUT_SECONDS = 15 * 60     # <-- สลับมาใช้ค่านี้ตอนใช้งานจริง (15 นาที)

# หมายเหตุ: เดิมมีค่า DEFAULT_COURSE_ID fix ไว้วิชาเดียวตรงนี้ แต่ตอนนี้ระบบ
# รองรับหลายวิชาพร้อมกันแล้ว โดยหา "วิชาที่กำลังเรียนอยู่ขณะนี้" จากตารางเวลา
# (ตาราง CourseSchedule) แทนโดยอัตโนมัติ ดู app/schedule_service.py

# ---------------------------------------------------------------------------
# เกณฑ์ "มาสาย" (Phase 5 - Report Export):
# นับว่า "มาสาย" ถ้าเช็คชื่อเข้าเรียนหลังจากเวลาที่อาจารย์กดปุ่ม "เริ่มคลาส"
# (ClassSession.started_at) เกินกว่าจำนวนนาทีนี้
# ---------------------------------------------------------------------------
LATE_THRESHOLD_MINUTES = 10

# ---------------------------------------------------------------------------
# แหล่งที่มาของภาพกล้อง (Camera Source):
# - 0  -> กล้องหลักของโน้ตบุ๊ก (ค่าเริ่มต้น)
# - 1, 2, ... -> กล้องตัวอื่น เช่น ถ้าลง "DroidCam Client" บน PC แล้ว มือถือจะ
#                กลายเป็นกล้องตัวที่ 2 ของเครื่อง ลองเปลี่ยนเป็น 1 ดูก่อน
# - "http://<ip มือถือ>:8080/video" -> ถ้าใช้ app "IP Webcam" (Android) สตรีม
#                ผ่าน WiFi ไม่ต้องลงโปรแกรมอะไรบน PC เพิ่ม ใส่ URL ที่ app
#                แสดงไว้ตรงนี้ได้เลย (ต้องเชื่อมต่อ WiFi เครือข่ายเดียวกันกับ PC)
#
# เหตุผลที่แนะนำกล้องมือถือ: กล้องบนโน้ตบุ๊กหลายรุ่นความละเอียด/เฟรมเรตต่ำ ทำให้
# face_recognition หน่วงหรือจับหน้าไม่ทัน กล้องมือถือมักลื่นกว่ามาก
# ---------------------------------------------------------------------------
CAMERA_SOURCE = 0

# ความถี่ที่ background monitor จะตรวจสอบสถานะ Away ทุก ๆ กี่วินาที
# ยิ่งค่าน้อย ยิ่งตรวจบ่อย (แจ้งเตือนไวขึ้น) แต่ก็กิน CPU มากขึ้นเล็กน้อย
MONITOR_POLL_INTERVAL_SECONDS = 2

# ---------------------------------------------------------------------------
# Debounce สำหรับ Video Stream (Auto Detect Mode - กล้องเดียว ไม่มีปุ่มสลับโหมด):
# ถ้านักศึกษาเดินผ่าน/ยืนหน้ากล้องนิ่ง ๆ กล้องจะจับภาพได้ ~30 เฟรม/วินาที
# ถ้าไม่หน่วงเวลา ระบบจะเรียก handle_face_detected() ทุกเฟรม ทำให้ตีความ
# "เข้า-ออก-เข้า-ออก" สลับกันไม่หยุดภายในไม่กี่วินาที
#
# ค่านี้กำหนดว่าต้องผ่านไปกี่วินาที ก่อนจะยอมให้ trigger event ซ้ำสำหรับ
# นักศึกษาคนเดิมได้อีกครั้ง ควรตั้งให้นานพอที่คนจะ "เดินผ่านกล้องจนสุด"
# (เดินผ่านประตูจริงมักใช้เวลา 2-4 วินาที จึงตั้งเผื่อไว้ที่ 8 วินาที)
# ---------------------------------------------------------------------------
CHECKPOINT_COOLDOWN_SECONDS = 8